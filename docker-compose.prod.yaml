version: '2'
services:
  passman-api:
    restart: always
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - MYSQL_ENDPOINT
      - MYSQL_USERNAME
      - MYSQL_PASSWORD
      - MYSQL_DB
    # ports:
    #   - "3000:3000"

  load-balancer:
    depends_on:
      - passman-api
    restart: always
    build:
      context: .
      dockerfile: Dockerfile.prod.nginx
    volumes:
      - /etc/letsencrypt:/etc/letsencrypt
    ports:
      - "80:80"
      - "443:443"